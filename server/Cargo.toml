[package]
name = "mixxx-config-server"
version = "0.1.0"
edition = "2024"

[dependencies]
tiny_http = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
quick-xml = { version = "0.36", features = ["serialize"] }

[profile.release]
opt-level = "z"
lto = true
strip = true
